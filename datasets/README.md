# datasets/ : mesures

Données brutes (banc, capteurs, calibration). **Immutables** : ne jamais modifier un fichier une fois versionné ; un nouveau traitement produit un nouveau fichier dans `results/`.

Convention : `AAAA-MM-JJ_EXP-XXX_description.{csv,parquet}` + un `README` par campagne (matériel, unités, repère, fréquence).
Aucune donnée réelle à ce jour.
