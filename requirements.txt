# Dépendances d'exécution (lues par setup.py → install_requires).
# Minimums = versions validées le 2026-09-24 (Python 3.10).
# Tkinter (GUI) est fourni par le système : apt install python3-tk
numpy>=1.21
matplotlib>=3.5
pybullet>=3.2
pyyaml>=5.4
